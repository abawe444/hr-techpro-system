# Sample workflow for building and deploying a Jekyll site to GitHub Pages
name: DISABLED - Jekyll GitHub Pages

on:
  # disable automatic runs; allow manual only
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Jekyll workflow disabled; use 'Deploy to GitHub Pages' instead."
